"""Config, logging, metrics, and the admin routes.

The operations surface. None of it makes molla faster and all of it is what
makes molla debuggable by somebody who is not holding the source open, which is
the only state it will ever be in once it leaves this repository.
"""
